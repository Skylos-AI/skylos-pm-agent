{
  "name": "skylos-pm-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Skylos PM Agent — internal project management for a 3-person Bolivian AI agency. Phase 1: database foundation.",
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init",
    "prisma:studio": "prisma studio",
    "prisma:seed": "prisma db seed",
    "import:base-unificada": "ts-node scripts/import-base-unificada.ts",
    "validate": "ts-node scripts/validate-schema.ts"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "csv-parse": "^5.6.0",
    "prisma": "^5.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  }
}
